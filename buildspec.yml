version: 0.2

phases:
  install:
    commands:
      - echo Installing Java 21
      - yum install -y java-21-amazon-corretto
      - java -version

  build:
    commands:
      - echo Build started
      - mvn clean package

artifacts:
  files:
    - target/*.jar