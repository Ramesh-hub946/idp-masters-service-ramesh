spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://master-name.cpg2c08agk8m.us-west-1.rds.amazonaws.com:5432/postgres
    username: Master_DB
    password: yntec123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: public
        format_sql: true
    open-in-view: false